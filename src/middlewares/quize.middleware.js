// const Teacher = require('../controllers/quize.controller');

// const isTeacher = (req, res, next) => {
//   if (req.user.role !== 'Teacher') {
//     return res.status(httpStatus.FORBIDDEN).json({ message: 'Access denied' });
//   }
//   next();
// };

// module.exports = isTeacher;
